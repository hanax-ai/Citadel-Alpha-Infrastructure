# Prometheus Configuration (Placeholder)
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'ollama-gateway'
    static_configs:
      - targets: ['localhost:8000']
        labels:
          instance: 'fastapi-gateway'
  - job_name: 'ollama-service'
    static_configs:
      - targets: ['localhost:11434'] # Ollama exposes /metrics
        labels:
          instance: 'ollama-service'
